const EscuelaIndex = () => {
  return (
    <main
      className={`flex min-h-screen flex-col items-center justify-between p-24`}
    >
      <div>ESCUELA (TEACHER)</div>
    </main>
  );
};

export default EscuelaIndex;
