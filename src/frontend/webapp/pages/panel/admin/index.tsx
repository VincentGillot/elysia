const AdminIndex = () => {
  return (
    <main
      className={`flex min-h-screen flex-col items-center justify-between p-24`}
    >
      <div>ADMIN (ADMIN)</div>
    </main>
  );
};

export default AdminIndex;
